<body>

  <% if current_user %>
    <%= render 'header_nav_authenticated' %>
  <% else %>
    <%= render 'header_nav_public' %>
  <% end %>

  <div id="body" class="outer-wrapper">
    <div class="inner-wrapper">
      
      <%# PUT YOUR CONTENT HERE - THIS CONTAINER IS 1200px WIDE WITH 10px of PADDING LEFT AND RIGHT %>
    <div class ="cardwrapper">
      <div class= "rowofcards">
        <h1 class = "heading"> Browse Teachers: </h2>
         <%= form_with(url: '/browse/teachers', method: 'post', local:true) do |form|%>
            <%= form.label "Search" %>
            <%= form.text_field :search_teach_firstname%>
            <%= form.submit "Go" %>
          <%end%>
        <%= render partial: 'pages/teacher_card', locals: {teachers_card_array: @teachers_card_array}%>  
      </div>
    </div>

    </div>
  </div>

  <%= render 'footer_public' %>

</body>
